$ function (formtable){
				  var prenom;
				  var nom;
				  var adresse;
				  var tel;
				
				
				JQuery(#maListe > tbody:last).append(<tr>id<\/tr> <tr>nom<\/tr> <tr>prenom<\/tr> <tr>adresse<\/tr> <tr>tel<\/tr>);
				{for (id=0; id <= #maListe.length; id= ++) }

				$ form(nom.getElementsByTagName(name='""')) ;
				this.value = value
				{
				  value = value.split('').getElementsByTagName('')
				$ class RegExp1 extends RegExp {[Symbol.replace](str) 
					{return RegExp.prototype[Symbol.replace].call(value, str, name)
					  }
					}
				} 
				
				$ form(prenom.getElementsByTagName(name='""')) 
				this.value = value
				{
				  value = this.split('').getElementsByTagName('')
				}
				$ class RegExp1 extends RegExp {[Symbol.replace](str) 
					{return RegExp.prototype[Symbol.replace].call(value, str, name)
					  }
					}
				} 
				  		
				$ form(email).push(encodeURIComponent(adresse) + '=' + encodeURIComponent(data[adresse]));
				$ form.'adresse'=value
				{
				  value = value.split('').getElementsByTagName('')
			      class RegExp1 extends RegExp {[Symbol.replace](str) 
					{return RegExp.prototype[Symbol.replace].call(value, str, name)
					  }
					}
				 				  
				$ form(tel) {
				tel.value = value	
				$ value.split('').getElementsByTagName('')
				$ class RegExp1 extends RegExp {[Symbol.replace](str) 
					{return RegExp.prototype[Symbol.replace].call(value, str, name)
					  }
					}